# Railway 직접 GitHub 연동 사용 - 이 워크플로우 비활성화
# Railway Settings > Source > Auto Deploy ON 으로 자동 배포됨
name: Disabled - Using Railway Direct GitHub Integration

on:
  workflow_dispatch:  # 수동 실행만 허용 (자동 비활성화)

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Railway auto-deploy handles this via GitHub integration"
