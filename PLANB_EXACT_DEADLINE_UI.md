# PLANB 기한관리 UI 재구성본

## 반영 방향
- 사용자 제공 `PlanB-Calendar-Setup(1).exe`의 Electron 렌더러 구조와 스타일 값을 기준으로 기한관리 UI를 다시 구성했습니다.
- 기존의 “PLANB 느낌” 재해석이 아니라, PLANB의 화면 비율·카드 구조·툴바·월/주/일 캘린더 레이아웃에 맞추는 것을 우선했습니다.
- 전체면허자시스템의 상단 주메뉴는 유지하고, `기한관리` 진입 후 하단 작업영역을 PLANB형 화면으로 사용합니다.

## 화면
- 44px PLANB형 내부 타이틀바
- 284px 좌측 사이드바
- 좌측 미니 달력
- `캘린더` 카드와 단일 `기한관리` 캘린더
- `할 일` 카드
- 66px 캘린더 툴바
- 월 / 주 / 일 전환
- 월간 그리드, 주/일 시간 그리드
- 검색창 및 Ctrl+F 포커스
- PLANB형 신규/수정/상세 모달

## 기한 종류 단순화
- 화면에는 휴업만료/대폐차기한 등 기존 `task_type` 분류를 노출하지 않습니다.
- 새 일정은 내부적으로 단일 `기한관리` 타입으로 저장합니다.
- 기존 데이터는 삭제하거나 변환하지 않습니다. 기존 레코드에 분류 접두사가 제목에 붙어 있으면 화면에서는 접두사를 숨기고 실제 제목만 보여줍니다.

## 기존 시스템 연동
- 기존 `/api/deadlines` CRUD를 그대로 사용합니다.
- 회원 상세의 기한 목록/기한 등록 연결을 유지합니다.
- DB 마이그레이션은 필요하지 않습니다.

## 배포
현재 운영 저장소에 이 프로젝트 내용을 반영한 뒤 `main` 브랜치에 커밋하면 기존 Railway 자동배포 설정을 그대로 사용할 수 있습니다.
