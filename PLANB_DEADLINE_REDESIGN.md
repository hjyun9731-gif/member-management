# PLANB 스타일 기한관리 개편

기존 전체면허자시스템의 `deadline_tasks` 데이터와 API는 유지하면서, 업로드된 **PlanB Calendar 2.0.0**의 화면/조작 구조를 기한관리 업무에 맞게 이식했습니다.

## 적용 내용

- PLANB 계열 UI 토큰 적용: 284px 사이드바, `#5B6CF0` 강조색, `#F4F6FB` 보조 배경, `#ECEEF4` 경계선
- 왼쪽 미니 달력
- 업무구분별 캘린더 표시/숨김 및 건수 표시
- 도래 기한을 PLANB의 `할 일` 패널 형태로 표시
- 월 / 주 / 일 / 목록 4개 보기
- 오늘 / 3일 이내 / 7일 이내 / 기한초과 / 완료 빠른 필터
- 전체 기한 검색 및 `Ctrl+F` 검색창 포커스
- 월간 셀의 `+` 또는 더블클릭으로 해당 날짜 기한 등록
- 기한 상세 / 수정 / 완료 / 삭제 / 대폐차 기간연장 기존 기능 유지
- 담당자 입력 필드 추가
- 회원 상세에서 기한 등록 시 `member_id` 연결값 보존
- 선택한 업무구분 표시/숨김 및 마지막 보기 상태를 브라우저 로컬 저장소에 보존
- 반응형 레이아웃 추가

## 데이터/서버 영향

DB 스키마 변경은 없습니다. 기존 `/api/deadlines` API와 `deadline_tasks` 데이터를 그대로 사용하므로 별도 데이터 마이그레이션이 필요하지 않습니다.

Google Calendar / Microsoft / CalDAV / Notion 같은 PLANB 외부 계정 연동 기능은 전체면허자시스템 기한관리와 무관하므로 포함하지 않았습니다. 대신 PLANB의 캘린더/할 일 UX가 실제 협회 기한 데이터에 연결됩니다.

## 주요 변경 파일

- `app/static/app.js`
- `app/static/styles.css`
- `PLANB_DEADLINE_REDESIGN.md`

## 검증

- `node --check app/static/app.js` 통과
- Python `app` 전체 구문 컴파일 통과
- 샘플 데이터로 월/주/일/목록 렌더 함수 실행 확인
- 임시 SQLite에서 기한 등록 → 목록 → 수정 → 기간연장 → 완료 → 요약 API 흐름 확인
